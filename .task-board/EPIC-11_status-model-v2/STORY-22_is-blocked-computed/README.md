# STORY-22: is-blocked-computed

## Description
Implement computed is_blocked flag from blocked-by dependencies. CLI should prevent starting development if blocked.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
