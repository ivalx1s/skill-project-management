# TASK-260206-xw3bhb: cursor-navigation-keys

## Description
Replace viewport with cursor-based navigation. Track selectedIdx over rows. Handle keys: j/down (next selectable row), k/up (prev selectable row), g (first selectable), G (last selectable). Cursor skips header rows. Ensure visible area follows cursor (manual scroll offset calculation based on window height).

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
