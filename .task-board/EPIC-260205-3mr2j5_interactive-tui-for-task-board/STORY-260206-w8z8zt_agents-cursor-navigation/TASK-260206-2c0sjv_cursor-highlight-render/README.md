# TASK-260206-2c0sjv: cursor-highlight-render

## Description
Update renderContent to accept selectedIdx. Highlight selected row with background color (subtle, like board cursor). Non-selected rows render normally. Header rows (agent name + separator) render without highlight capability. Return []string of rendered lines for viewport.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
