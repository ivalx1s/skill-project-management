# TASK-260206-22jyak: agents-row-data-model

## Description
Create agentRow struct with fields: kind (header/element/child), agentName, element pointer, child pointer, displayLine string. Add buildRows() method on AgentsModel that flattens agents into []agentRow. Rows are rebuilt on every data load. Header rows are non-selectable (cursor skips them).

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
