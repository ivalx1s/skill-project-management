# TASK-260206-2tovz9: board-custom-filter

## Description
Replace bubbles/list filter with custom implementation. Use textinput from bubbles for input (or reuse command palette approach). Filter FlatNode list by ID+name+status (same as FilterValue). When filter active: expand all nodes, show only matching rows. When filter dismissed: restore expand state. Track filterActive/preFilterExpandedIDs like current wasFiltering logic. Command palette /filter triggers custom filter mode.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
