# STORY-260205-23ol4w: mouse-zones-click-detection

## Description
Implement proper mouse click detection using lipgloss zones. Currently click just toggles selected item. With zones: click on specific item should select and toggle it. Requires refactoring list rendering to wrap each item in zone.Mark(id, content) and use zone.Get(x, y) on click.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
