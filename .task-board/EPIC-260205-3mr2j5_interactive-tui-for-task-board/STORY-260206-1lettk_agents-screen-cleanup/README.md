# STORY-260206-1lettk: agents-screen-cleanup

## Description
Remove agents screen code duplication. Currently 3 files with same logic: command.go (types + loading), agents.go (model/view), internal/ui/screens/agents/agents.go (full duplicate). Consolidate into one clean implementation.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
